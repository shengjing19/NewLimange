server:
  port: 8231

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/limange?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: limange
    password: limange_Hisx1212
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate # 自动更新表结构，生产环境必须 validate  开发环境update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  upload-path: /limange_imgs/uploads # 文件存储路径